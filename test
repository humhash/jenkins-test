# second changes done
# Add one more line
const axios = require('axios');

const url = 'http://your-jenkins-url/github-webhook/';
axios.post(url)
  .then(response => {
    console.log(response.status);
  })
  .catch(error => {
    console.error(error);
  });
